#Question 3
# => "pumpkins"
#=> ["pumpkins", "rutabaga"]
# The string concatenation doesn't change the string variable because it doesn't
# mutate the caller. Rather, it creates a new object in memory. However, with
# with the << method for the array, it doesn in fact mutate the caller.

# Question 5
# def color_valid(color)
#   color == "blue" || color == "green"
# end


